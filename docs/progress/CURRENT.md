# Current state — September 20, 2026

- **Objective:** combined progressive braking, flat-ground drive and experimental CH11 lowering are deployed. Powered stationary feedback passed; physical movement tests of the new behavior remain Austin's next step.
- **Workspace/publication:** root control checkout on `codex/wifi-ota` integrates deployed `codex/drive-braking` release record `eb2bb23` while preserving the root documentation and all-agent GitHub publication history. The eight project branches have published checkpoints. No Internet service deployment.
- **Installed at last verification:** source `43b1967dd03d26d8b2ccf6a698901c56272039d2`; application 1,194,224 bytes, whole-file SHA-256 `a3b1e64c109bfc91eb43842769efebefbc9b32d16baea290de0af2f6b7a8b982`. Verified app1, ESP image digest `bc1e158acac24fa08a9fb81b26933b00243baa71f67c106c9696135182f0a9b3`, build `Sep 19 2026 23:44:38`. Query `/api/info` for the live slot; OTA can change it.
- **Release evidence/package:** [manifest and checks](../../evidence/balance-drive-braking/README.md), [postflight](../../evidence/balance-drive-braking/ota-postflight.json). Exact private package remains at `worktrees/drive-braking/artifacts/drive-braking-v5/release/` relative to the project root. Previous Wi-Fi rollback application remains at `artifacts/wifi-ota/release/`; original full-flash backup remains local. Credentials and credentialed binaries are not published.
- **Access:** joins SvensHaus at [hopscotch.local](http://hopscotch.local/); last verified DHCP IP [192.168.1.172](http://192.168.1.172/). Use [Wi-Fi/OTA operations](../WIFI_OTA.md) and `scripts/robot_wifi.py` for live state, validated `.csv`/`.wire` export and application OTA. USB remains for tuning, calibration and recovery. Never use `uploadfs`.
- **Behavior:** [braking v5](../BALANCE_DRIVE_BRAKING_2026-09.md) smoothly increases reference deceleration from 8 to 20 rad/s² at higher speeds while retaining the gentle low-speed finish and v4 arm amplitude. [Ground drive](../GROUND_DRIVE_2026-09.md) follows controller ownership rather than CH7 selection alone. A fresh CH11 pulse during settled balance requests [experimental supported lowering](../BALANCE_LOWER_2026-09.md), requiring a restrained first trial and mechanically verified reach/clearance. Startup/neutral control regression traces are unchanged.
- **Control protection:** core-0 networking, bounded RAM snapshots and control-owned disarmed flash/association maintenance remain unchanged. No Wi-Fi motion/tuning endpoint. Both arm switches must be observed low before rearming after boot/maintenance. Automatic boot rollback is unavailable.
- **Root integration:** production `src/`, scripts, tests, build configuration, dashboard and radio files match installed source `43b1967` exactly; the root merge independently passed all 10 native / 27 Python / radio / dashboard / pinned-build checks. Later handset Lua v3 source/installation records (`9c5b88d`, `c72d67b`) are separately preserved on `codex/radio-telemetry`.
- **Software/model verification:** final combined 10 native executables, 27 Python tests, pinned ESP32 build and syntax/whitespace checks passed; radio/dashboard checks passed. All 81 lowering model cases were rerun. Braking screening retained 324 identical neutral/startup traces and added no falls in 672 paired drive plus 192 stop comparisons; baseline failures remain. Models do not establish physical reliability or stopping distance.
- **OTA result:** first two attempts aborted with the previous app0 identity intact. A 1 KiB/50 ms paced transfer with 120-second socket timeout succeeded in 62.8 seconds. The transmitter was turned off near its end; timeout, pacing and radio state changed together, so the failure cause is unisolated. [Upload evidence](../../evidence/balance-drive-braking/ota-slow-retry.txt).
- **Powered postflight:** at 2026-09-20 03:56:33 UTC, all six motors online, fresh, disabled and error-free; both groups disarmed, 23.65 V, IMU fault 0, maintenance false and rearm required. Transmitter was off. No movement was initiated by the release task.
- **Preservation/telemetry:** latest physical v4 run re-downloaded with all 2,981 CSV rows and original wire bytes identical. New logs remain schema 4 / 240 bytes with feature flags 4095; old logs retain their own metadata. Application-only OTA preserved the filesystem; calibration was not directly re-read through the web API.
- **Next:** turn the transmitter on with CH9/CH10 low, CH11 released and sticks centered; confirm fresh RC/motor/IMU state. Follow [balance testing](../BALANCE_TESTING.md) for a supervised moderate-input braking trial, download after disarming, then evaluate CH11 lowering separately using its restraint/clearance procedure.
