# Current state — September 20, 2026

- **Objective:** Wi-Fi telemetry and OTA are deployed with progressive braking v5, ground drive and experimental forward-fall/catch lowering v2. Austin reports driving worked well. Both lowering versions failed physical trials; hold further CH11 attempts while the lowering owner diagnoses v2.
- **Installed at last verification:** source `dd74154aa3f534f95506b5c0dd66e1c6d901e091`, app0. Application 1,196,112 bytes; whole-file SHA-256 `71ca02be6f0471a63f92605f6ae0204acd45a58539a5eec7da0e2328a9542d72`; ESP image digest `7668a0215df34b7e5c0030a23705ba8016343260d1e6bab7aacfe202897c7190`. The unchanged network translation unit retains build string `Sep 19 2026 23:44:38`; use the digest and slot for identity.
- **Deployment/package:** [verified deployment](../../evidence/ota-lowering-v2/README.md), completed 2026-09-20 18:12:21 UTC. Frozen private package remains in `worktrees/drive-braking/artifacts/lowering-v2/candidate/` relative to the project root. Its immutable preparation manifest still says `queued_not_installed`; the deployment record supersedes that status. Previous v5/v1 package remains in `worktrees/drive-braking/artifacts/drive-braking-v5/release/`; the earlier Wi-Fi package and original full-flash backup also remain local. Credentials and configured binaries are excluded from GitHub.
- **Access/workflow:** joins SvensHaus at [hopscotch.local](http://hopscotch.local/), last verified DHCP IP [192.168.1.172](http://192.168.1.172/). [Wi-Fi/OTA operations](../WIFI_OTA.md) use `scripts/robot_wifi.py` for live state, validated CSV/wire export and a single-command verified deployment. Reuse completed checks and exact frozen bytes. USB remains for tuning, calibration and recovery. Never use `uploadfs`; automatic boot rollback is unavailable.
- **OTA limitation:** two identical paced transmitter-on uploads aborted without rebooting or changing the old app1 image. With the transmitter off, the same 1 KiB/50 ms, TCP_NODELAY, 120-second-timeout transfer succeeded in 62.759 seconds. The cause is unconfirmed. The helper now automates pacing, manifest/preflight checks, saved-run preservation, running-image/health verification and discovery after a lost acknowledgment. Repeating an already-installed image verifies without flashing again. Its 10 host tests pass; the refactored CLI has not had a separate hardware trial.
- **Powered postflight:** six motors online, fresh, disabled and error-free; both groups disarmed, healthy IMU, maintenance false and rearm required. Transmitter was off. The previous 2,371-row CSV and exact wire were unchanged across OTA. Application-only OTA preserved the saved filesystem log; calibration was not independently re-read through the web API.
- **Current telemetry:** new logs use schema 4 / 240 bytes / feature flags 8191. V2 phase 9 is forward initiation and phase 10 is catching; the lowering rate column uses the fast 6 ms filter. Historical logs retain their original feature metadata and interpretation. Live JSON is separate from the complete onboard capture.
- **Physical v2 evidence:** [1,984-row, 39.760-second archive](../../evidence/balance-lower/trial-v2-20260920/README.md), checkpoint `d58c917`. Fast arm extension began after only 0.049° of forward displacement; first inferred impact occurred while arms were still moving about 1.3 rad/s. In the final 20 ms, measured body rate reversed from −28.789 to +42.163°/s and ended `lower_wrong_direction` before sustained support. The log ends at the fault, before the later reported fall. Inner-loop maximum interval was 5,503 µs. Replay seeded at commitment matches the subsequent phases; full CH11 replay does not reproduce preparation confirmation, and that limitation is retained.
- **Earlier v1 evidence:** [2,371-row failed trial](../../evidence/balance-lower/trial-20260920/README.md) never qualified support, canceled reach and ended `lower_motion_limit`. Its complete phase replay matches. The first model assumed stable preparation and missed the coupled failure; the v2 contact model also understates moving-arm impact. Model screening does not establish physical reliability.
- **Source/verification:** root production source, dashboard, radio and build configuration match installed `dd74154`. Release record [e9e1c65](https://github.com/amcchord/Hopscotch/blob/e9e1c65996c5f6539a704447516834f2ce12527c/evidence/lowering-v2-integration/README.md) passed 10 native executables, 27 Python tests, radio/dashboard checks, pinned build, 229 model cases and the v1 replay. Those unchanged checks were reused. Root independently checked source identity and the new helper's 10 host tests. Existing event-core macro warning remains. Later handset fixes/install records stay on `codex/radio-telemetry`, outside this firmware-source snapshot.
- **Control protection:** core-0 networking, bounded RAM snapshots and control-owned disarmed flash/association maintenance remain unchanged. No Wi-Fi motion/tuning endpoint. Both arm switches must be observed low before rearming after boot/maintenance. [Driving/braking](../BALANCE_DRIVE_BRAKING_2026-09.md) and [ground-drive ownership](../GROUND_DRIVE_2026-09.md) are unchanged by lowering v2.
- **Owners/publication:** root `codex/wifi-ota` owns integration, shared records and all-agent GitHub publication. `worktrees/balance-lower` / `codex/balance-lower` owns the failed v2 log, revised lowering model/candidate and current device access. `worktrees/ota-reliability` / `codex/ota-reliability` owns transmitter-on OTA reliability; its in-progress network changes are separate from the installed source. `worktrees/drive-braking` retains the validated release packages. `codex/radio-telemetry` checkpoint `05e25ab` records verified Lua v3.1 SD installation; handset boot confirmation is pending. No Internet service was deployed.
- **Published OTA candidate — not installed:** `codex/ota-reliability` source checkpoint `67f3746` adjusts eligible upload timeouts, TCP behavior, telemetry offers during OTA and transfer diagnostics. Its owner reports focused transport-harness, native/Python, radio/dashboard and pinned-build checks passing; physical transmitter-on success remains unverified. It is deliberately outside the installed-source integration pending the owners' coordinated release.
- **Next:** lowering and OTA owners develop focused fixes in their own checkouts, then hand off explicit candidate checkpoints for a coordinated release. Keep installed v2 lowering retries on hold. This root integration made no robot requests, firmware upload, settings change, restart, arming or motion.
