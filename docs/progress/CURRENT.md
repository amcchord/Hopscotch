# Current state — September 20, 2026

- **Objective:** revised CH11 forward-fall/catch v3 is installed for Austin's restrained manual trial. Ground/standing driving worked well in the earlier trial and its code is preserved. Lowering v1/v2 failed; v3 has not yet had a physical motion trial.
- **Installed:** source `8449ddb5758d60d6b3b3e84702bef4d386b06791`, app1, verified 2026-09-20 19:01:29 UTC. Application 1,197,808 bytes; whole-file SHA `53a574a75a0122b0aa3a16567c79bd282631b232c72184c7c3e836c53f51e0f7`; ESP digest `45e7a83cd649e4a1e5182058e3cfb9e4cc80fad4cda27be4a2c9f4465451ab11`. [Deployment/evidence](../../evidence/ota-lowering-v3/README.md), [v3 behavior/trial](../BALANCE_LOWER_V3_2026-09.md).
- **Package/recovery:** private configured binary/ELF/partitions in `worktrees/balance-lower/artifacts/lowering-v3-export/candidate/` relative to project root. Manifest's queued status is preparation history; deployment record establishes installation. Previous v2 package remains `worktrees/drive-braking/artifacts/lowering-v2/candidate/`, v5/v1 in `worktrees/drive-braking/artifacts/drive-braking-v5/release/`; original Wi-Fi/full-flash backups remain local. No credentialed binaries or private headers were copied between worktrees or published. No automatic boot rollback; never uploadfs.
- **Motion changes:** slightly shorter preparation (1.25 rad), gentler launch (2 rad/s²), fast catch requires 0.1° measured forward drop and forward rate. First qualifying contact stops both forward strokes; bounded retreat begins on measured deceleration. Both arms still need independent support evidence. All ordinary drive/startup/radio/dashboard production files are byte-identical to installed v2; [source comparison](../../evidence/ota-lowering-v3/source-preservation.json).
- **Model/validation:** [304 paired cases](../../evidence/balance-lower/first-impact-v3/README.md): 231→248 complete, 21 improve, four regress; all 11 injected failures reject completion. Asymmetric contact and higher impact rates remain limits. Combined native/Python (10/37), radio/dashboard, OTA transport harness and pinned build passed. Export-only follow-up passed the 6,000-row yield/byte/checksum regression, 10 updater host tests and final configured build. Reused unchanged model and radio/drive checks.
- **Physical evidence:** v2 rebound [1,984-row trial](../../evidence/balance-lower/trial-v2-20260920/README.md) first contacted while arms still moved about 1.3 rad/s, then reversed body rate and faulted. A later [4,789-row run](../../evidence/ota-lowering-v3/recovered-run-summary.json) ends `bailout_angle_error`; it never entered forward commitment/catching, so it is a separate record.
- **OTA/export:** transport version 2 permits brief receive gaps, pauses new dashboard offers during upload and exposes failure progress. Transmitter-on hardware acceptance remains pending; transmitter-off bootstrap succeeded in 62.842 seconds. The initial updater failed before upload because the old long-log export reset with task-watchdog reason 6. Application-only recovery preserved the filesystem and recovered the valid 4,789-row log using cooperative export. No pre-update hash was available; this documented recovery exception is not the routine workflow. Austin also allowed discarding that log, which proved unnecessary.
- **Health/access:** six motors powered, online, fresh, disabled and error-free; both groups disarmed, fresh IMU, released maintenance, rearm interlock retained. Joins SvensHaus at [192.168.1.172](http://192.168.1.172/) / [hopscotch.local](http://hopscotch.local/). [One-command OTA procedure](../WIFI_OTA.md) validates the frozen package, backs up the run, paces upload, verifies identity/health and compares the saved run; reuse checked bytes. No motion/configuration/calibration was initiated.
- **Ownership/integration:** `worktrees/balance-lower` / `codex/balance-lower` owns device access and this installed release record. The OTA fork task completed `67f3746`/`50dfdf2`, merged here; that task and the root integration task are now archived. Root checkout stays at `045e956` on `codex/wifi-ota`; this branch includes that history plus v3/export fixes. No other worktree was edited. Standing-drive/radio tasks retain their own branches and packages; handset boot confirmation remains separate.
- **Next:** Austin can turn the transmitter on with both arm switches LOW/CH11 released for a disarmed full reinstall with a deliberate four-second gap. Then do one restrained v3 lowering trial and retrieve its log before another attempt. No claim of physically reliable or graceful lowering yet.
