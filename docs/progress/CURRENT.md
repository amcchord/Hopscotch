# Current state — September 19, 2026

- **Objective:** Wi-Fi telemetry/OTA implementation and motor-power-off bench verification are complete. Project docs now use OTA and wireless telemetry as the default workflow. Powered stationary verification remains the next operator step.
- **Workspace:** project/control checkout `/Users/austinmcchord/Development/Hopscotch`, local branch `codex/wifi-ota`; implementation commits `694bd47`, `78bf920` and `6018cd4`, based on `112e7e7`. Radio worktree untouched. Austin authorized publication; all seven local branches are now on GitHub with matching checkpoint commits, including in-progress agent work. No Internet service deployment.
- **Installed at last verification:** source `6018cd4`, Wi-Fi/OTA application in app0, 1,187,632 bytes; SHA-256 `3bd85bfdd26f48df86c5d87b76797282f4c2479a7e96b2838217f479553171cf`. Frozen package `artifacts/wifi-ota/release/`; full original 8 MiB backup in `artifacts/wifi-ota/`. Query `/api/info` for the live slot; OTA alternates slots.
- **Access:** joins SvensHaus at [hopscotch.local](http://hopscotch.local/); last tested DHCP IP [192.168.1.172](http://192.168.1.172/). Embedded dashboard, live telemetry, checked saved-run download, authenticated disarm/OTA/reconnect. Private credentials and device token are in gitignored `src/network_secrets.h`. CLI: `scripts/robot_wifi.py`.
- **Concurrent checkpoints:** ground-drive source `b323220` / validation `1337c10` passed nine native executables, 27 Python tests and a full build. Drive-braking `749e562` / raw-capture preservation `59895c7` is explicitly WIP, not a deployable release. The standing-drive branch has also cherry-picked the ground-drive work and current docs; its braking work remains in progress and integration/testing belongs to its owner.
- **Operating docs:** [Wi-Fi / OTA guide](../WIFI_OTA.md) covers updates, API, `.csv`/`.wire` downloads, analysis and recovery; [balance testing](../BALANCE_TESTING.md) covers untethered trials. USB remains for tuning/calibration and recovery. Never use `uploadfs`, including for UI changes. Dated balance reports/evidence retain historical results with current-workflow pointers.
- **Control protection:** all networking, including SDK Arduino events, runs on core 0. Fixed-size RAM snapshots leave the control task; networking never formats or waits on core 1. Flash and association require a control-owned disarmed interlock; arming is inhibited throughout. Boot requires both RC arm switches observed low. Radio remains the motion-control path.
- **Balance baseline:** driving damping v4 (`a2774d4`) remains unchanged; startup/stationary behavior, driving constants, channels and motor setup retained. See [v4 findings](../BALANCE_DRIVE_DAMPING_2026-09.md). Actual powered validation of this combined release is pending.
- **Preservation:** initial application readback matched its package; entire LittleFS, bootloader and partition table matched the pre-upgrade backup byte for byte. The final TCP callback fix was installed by application-only OTA with a verified running-image digest. Trim 3.33°, calibrated arm deltas and 571,828-byte schema-v4 saved log retained. Wireless export validates all 2,381 samples and matches pre-upgrade rows.
- **Verification:** software/native/Python/radio checks, bidirectional OTA/reboot, checksum/image/auth rejection, interrupted update and disarmed Wi-Fi reconnect passed. See [release evidence](../../evidence/wifi-ota/README.md) and [operating guide](../WIFI_OTA.md). A heavy-load TCP-library crash was reproduced, decoded and fixed with a source-hash-checked patch plus regression tests; final load-test details are recorded in the release evidence.
- **Limits:** OTA does not provide automatic boot rollback. Web settings/CAN mutation routes are disabled; configuration uses the USB console. No Wi-Fi motion commands or Internet deployment. No powered movement tests were performed.
- **Final checks passed:** 61 s normal telemetry (~5.7 Hz) and 122 s with three readers, one stalled socket and 586 HTTP requests: zero transport errors, disconnects, resets or 200 Hz intervals above 7.5 ms. Balance/control maxima 5.496/5.835 ms. Final reconnect and interrupted-upload checks also passed. RC-link quality was excluded at Austin's request while he adjusted the radio. Calibration entered from held CH11 was exited without saving; calibrated deltas were verified unchanged.
- **Next:** Austin can power motors for a stationary feedback check with both arm switches low and CH11 released; leave both groups disarmed. Verify six fresh motor responses, voltage/current and faults before any movement. Powered balance and RC readiness remain separate supervised checks.
