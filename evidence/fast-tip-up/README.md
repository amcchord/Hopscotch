# Fast tip-up candidate handoff — September 20, 2026

**Release status update:** source `3814f1f` was merged into `e55cecb` and
[installed/verified](../lowering-v4-fast-integration/README.md). The handoff below
records preparation history; fast motion remains physically untested.

**Preparation status at handoff: validated source candidate; queued for the lowering owner's next OTA.
No fast physical test or deployment was performed here.**

Project/repository: Hopscotch. Task checkout `worktrees/fast-tip-up`, branch
`codex/fast-tip-up`, baseline `400c98d` (installed v3/export/OTA fixes included).
The other active task owns `worktrees/balance-lower` / `codex/balance-lower`,
device access, shared records, final combined validation and deployment.

The feature adds CH6 high/low selection, a finite 2.6-second tip-up trajectory,
measured quiet capture and tracking/freshness/motion guards. The old slow
trajectory and all ordinary balance, drive, return and lowering control are
preserved. [Behavior and operator notes](../../docs/archive/2026-09/FAST_TIP_UP_2026-09.md).

## Evidence

- [Analysis](analysis.json): four archived traces, file hashes, 8.823–8.834 s
  slow tip-up timing, torque/lag observations and bounded kinematic inference.
- [Trajectory](trajectory.csv): generated by the production C++ policy using
  `tests/test_balance_tip_up.cpp --dump`, with a simple arm tracking fixture.
  This is synthetic data, not a recording from the robot.
- [Comparison](tip-up-comparison.png): archived body angles and new targets.
- [Validation](validation.json): commands, check results and source preservation.

All eleven native executables and 37 Python tests passed, including 162 new
tracking cases plus startup rejection, moving-body/arm capture rejection,
latched failures, individual blocked arms, fresh input loss, nonfinite input,
clock rollover and control-gap checks. The maximum modeled capture time is
2.74 s, generated speed 2.06 rad/s and acceleration 2.44 rad/s². These tests
exercise the real portable trajectory policy; the plant fixture does not
simulate contact dynamics or certify physical success.

The pinned ESP32 build passed using the project's existing Python 3.13
PlatformIO environment. An initial build correctly refused the absent private
network header in this isolated checkout. The successful build adds an include
path to the existing project-local private header; no credentials were copied.
Configured binary/ELF and full logs remain in ignored `.pio/` and `output/`.
The existing `ARDUINO_EVENT_RUNNING_CORE` macro warning remains.

## Reproduction

From this checkout, compile/run the fast policy and data screen:

```sh
mkdir -p output
clang++ -std=c++17 -Wall -Wextra -Werror -Isrc tests/test_balance_tip_up.cpp -o output/test_balance_tip_up
output/test_balance_tip_up
# Use a Python environment with numpy and matplotlib; read the latest source in place.
python scripts/analyze_fast_tip_up.py --latest ../balance-lower/telemetry_logs/bal_20260920_forward_catch_v3_wifi.csv
```

The common `scripts/check_balance_candidate.sh` includes the new test; the
integration owner should run it once for the final combined source using the
validated private-header build configuration. Reuse the unchanged slow log
archives and focused evidence; no device access is needed for source checks.

## Next OTA handoff

Cherry-pick the candidate commit identified in this task's handoff message into
the owned lowering integration checkout. Preserve installed v3/export/OTA
fixes and merge the independently developed lowering v4 changes. The only
expected source overlap is `src/balance_controller.cpp`: combine the header
feature flags to **65535**, retain independent fast decoder metadata for bit
16384, retain lowering v4 metadata for bit 32768, and retain `pilot_flags` bit
128 for fast-run selection alongside lowering active/phase bits. No binary
schema enlargement is needed. Keep the new native test in the consolidated
check script.

Freeze and validate the combined source, build/package its own configured
image, and use the existing verified OTA procedure. This task made no robot
requests, settings writes, arming, movement, upload or shared-history push.
Physical confirmation of the intended roughly three-second lift remains the
next operator check after that OTA.
